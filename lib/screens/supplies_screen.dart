import 'package:flutter/material.dart';

class SuppliesScreen extends StatelessWidget {
  const SuppliesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tela de Insumos', style: TextStyle(fontSize: 20)),
    );
  }
}
